import { Routes } from '@angular/router';
import { EventsComponent } from './events/events';

export const routes: Routes = [
  { path: '', component: EventsComponent },  // homepage shows listing
];

